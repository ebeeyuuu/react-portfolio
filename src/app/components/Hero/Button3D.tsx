"use client"
import React, { useState } from 'react';

const Button3D: React.FC = () => {
  return (
    <button id="btn" className="mt-[20px] font-semibold text-lg">
      Learn more
    </button>
  );
};

export default Button3D;
